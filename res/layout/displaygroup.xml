<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/blue" >

    <ListView
        android:id="@+id/listviewGroups"
        android:layout_width="match_parent"
        android:layout_height="393dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
		
        android:layout_below="@+id/createGroupButton" >

    </ListView>

    <Button
        android:id="@+id/createGroupButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:onClick="onCreateGrpButtonClick"
        android:text="Create Group" />

    <EditText
        android:id="@+id/filter"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/listviewGroups"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_toLeftOf="@+id/createGroupButton"
        android:ems="10"
        android:hint="Filter Results" >

        <requestFocus />
    </EditText>
    
</RelativeLayout>
